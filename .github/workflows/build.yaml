name: CI - dev
on:
  workflow_call:
    inputs:
      appType:
        required: true
        type: string
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello ${{ inputs.appType }}"